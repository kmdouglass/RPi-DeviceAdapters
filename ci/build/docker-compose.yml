---
version: '3'
services:
  rpi-build:
    build: .
    image: kmdouglass/rpi-micromanager:build-20180922
    volumes:
      - /opt/rpi-micromanager:/micro-manager/src
    # first true is for configure, second is for patch
    command: /micro-manager/setup true true
